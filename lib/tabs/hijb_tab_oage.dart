import 'package:flutter/material.dart';

class HijbTabPage extends StatelessWidget {
  const HijbTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
