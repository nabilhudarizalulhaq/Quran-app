import 'package:flutter/material.dart';

class ParaTabPage extends StatelessWidget {
  const ParaTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
