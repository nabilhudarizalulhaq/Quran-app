import 'package:flutter/material.dart';

class PageTabPage extends StatelessWidget {
  const PageTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
